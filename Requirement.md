I want to create a project manager stand alone software for windows / linux / macos 
left side must be explorer where i can add multiple projects.
when i click on a project it should open the project details in the right side.

there should be a search bar at the top of the explorer to search for a project.
there should be a button to add a new project.

add new project needs to be a simple add new folder and user can write the name of project just like we do in windows explorer (It says new folder and user renames the folder)
These folders need to be like parent folder and child folders.
Each folder can have multiple folders inside it. and it can also have files inside it.

When user double clcik on folder it should open the folder in the right side.
When user click on file it should open the file in the right side.

we can also add custom icons to the folders and files.

we need to save the database in the system. or the local storage of the system.
the software will be used only by me so no need for cloud storage.
these data will be stored as .db file in the system. by default it should be in the documents folder. we can also change the location.

user needs to signin with gmail or google account. 

when a document is created it should be created directly in the drive of that google account that user has signed in with. 
we need a dedicated folder in that drive specifically for this software so documents and files created and edited viewed will be from that folder only and not other files and folders from the drive. inside this main folder we can create project wise folders and files.

for viewing files we need a viewer in the software itself. 
for image files we can use the default image viewer of the system. or we can use the default browser to view the image. 
for text files we can use the default text editor of the system. or we can use the default browser to view the text file. 
for pdf files we can use the default pdf viewer of the system. or we can use the default browser to view the pdf file. 
for other files we can use the default application of the system to view the file. or we can use the default browser to view the file.  